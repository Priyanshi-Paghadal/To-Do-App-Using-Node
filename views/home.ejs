<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.2.0/remixicon.css"
        type="text/css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .flex {
            display: flex;
        }

        .justify-between {
            justify-content: space-between;
        }

        .inner {
            width: 600px;
            background-color: #fff;
            min-height: 500px;
            border-radius: 10px;
            box-shadow: rgba(50, 50, 93, 0.25) 0px 2px 5px -1px, rgba(0, 0, 0, 0.3) 0px 1px 3px -1px;
        }

        .main {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #D1E9F6;
        }

        h1 {
            text-align: center;
            background-color: #4982a5;
            color: #fff;
            padding: 10px 0;
            border-radius: 10px;
        }

        ul {
            list-style: none;
        }

        input[type='submit'] {
            background-color: green;
            color: #fff;
            border: none;
            padding: 8px 35px;
            border-radius: 5px;
        }

        form {
            padding: 30px;
            justify-content: space-between;
        }

        label,
        p {
            font-size: 17px;
        }

        input[type='text'] {
            padding: 5px 10px;
            width: 330px;
        }

        input[type='checkbox'] {
            width: 17px;
            margin-right: 30px;
            accent-color: green;
        }

        p {
            border-left: 3px double #f6dbd8;
            height: 50px;
            padding-left: 20px;
            line-height: 50px;
        }

        a {
            display: inline-block;
            text-decoration: none;
        }

        ul {
            padding: 0 40px;
            border-block: 1px solid #f2f0f1;
            align-items: center;
        }

        .edit {
            background-color: #4567b7;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
        }

        .remove {
            background-color: #444;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            margin-left: 10px;
        }
    </style>
</head>

<body>

    <div class="main">
        <div class="inner">
            <h1>To-Do App </h1>
            <form action="/addData" method="post" class="flex">
                <div>
                    <label>Add Task : </label>
                    <input type="text" name="name">
                </div>
                <input type="submit" value="ADD">
            </form>

            <% allTask.map((item)=>{ %>
                <ul class="flex justify-between">
                    <li class="flex">
                        <input type="checkbox" name="check" class="check">
                        <p class="text">
                            <%= item.name %>
                        </p>
                    </li>
                    <li>
                        <a href="/edit/<%= item._id %>" class="edit">
                            <i class="ri-edit-2-line"></i>
                            Edit</a>
                        <a href="/delete?id=<%= item._id %>" class="remove">
                            <i class="ri-delete-bin-6-line"></i>
                            Remove</a>
                    </li>
                </ul>
                <% }) %>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js" type="text/javascript"></script>
    <script>

        $(document).ready(function () {
            $('input[type="checkbox"]').on('change', function () {
                if ($(this).is(':checked')) {
                    $(this).next('.text').css("text-decoration", "line-through");
                } else {
                    $(this).next('.text').css("text-decoration", "none");
                }
            });
        });

    </script>

</body>

</html>